[project]
name = "reel-rank-scripts"
version = "0.1.0"
description = "Result analysis and plotting tooling for ReelRank experiments"
requires-python = ">=3.11"
dependencies = [
    "pandas==2.2.3",
    "matplotlib==3.9.2",
]

[tool.uv]
package = false
